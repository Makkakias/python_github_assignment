Study Time Tracker
This program asks this user their study time today and then multiplies it by 7 to give a weekly total. It converts the input into a float so it can execute the calculation. The code also includes error handling so that the user inputs a valid number.
python app.py